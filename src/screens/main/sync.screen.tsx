import {Box, Text} from 'native-base';
import React from 'react';

type Props = {
  navigation: any;
};

const Sync = ({navigation}: Props) => {
  return (
    <Box>
      <Text>Syncronizing screnn</Text>
    </Box>
  );
};

export default Sync;